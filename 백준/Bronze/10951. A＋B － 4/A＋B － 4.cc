#include <iostream>

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    
    int A, B;

    while (std::cin >> A >> B ) {
        std::cout << A + B << "\n"; 

    }

    return 0; 

}